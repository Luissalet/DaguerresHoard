[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    model: uses the real CLIP model (downloads about 600 MB on first run); opt in with -m model
addopts = -m "not model"
